[package]
name = "wasmtriggers-macros"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
quote = "1.0.45"
syn = { version = "2.0.117", features = ["full"] }
wasmtriggers-core = { version = "0.1.0", path = "../wasmtriggers-core" }

[dev-dependencies]
trybuild = "1.0.103"
